# Copy to .env and fill in. The app reads DATABASE_URL from the environment.
# PostgreSQL connection used by both the app (read-only) and the pipeline (writes).
DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/plates

# Optional: cache TTL (seconds) for the app's data-access layer.
CACHE_TTL=3600
